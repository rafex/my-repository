name: Build and Deploy Debian Package CURP Verifier

on:
  push:
    branches: [ main ]
    paths:
      - "src/rust/curp_verifier/**"
      - ".github/workflows/curp-package-deploy.yml"
  workflow_dispatch:

jobs:
  build-and-deploy-amd64:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout repo
        uses: actions/checkout@v3

      - name: 🦀 Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: 📦 Install packaging tools
        run: sudo apt update && sudo apt install -y dpkg-dev

      - name: 🛠 Build Rust library
        working-directory: src/rust/curp_verifier
        run: make all

      - name: 📦 Build .deb package
        working-directory: src/rust/curp_verifier
        run: |
          make install DESTDIR=$(pwd)/deb_pkg
          mkdir -p deb_pkg/DEBIAN
          cp packaging/deb/amd64/control deb_pkg/DEBIAN/control
          dpkg-deb --build deb_pkg curp-verifier-amd64.deb
          ls -lh curp-verifier-amd64.deb
          ls -lah

      - name: 🚀 Upload to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.RAFEX_SERVER_HOST }}
          username: ${{ secrets.RAFEX_SERVER_USER }}
          key: ${{ secrets.RAFEX_SERVER_SSH_KEY }}
          source: "src/rust/curp_verifier/curp-verifier-amd64.deb"
          target: "/srv/repo/debian/dists/stable/main/binary-amd64/"
          debug: true

      - name: 🔄 Regenerate Packages.gz
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.RAFEX_SERVER_HOST }}
          username: ${{ secrets.RAFEX_SERVER_USER }}
          key: ${{ secrets.RAFEX_SERVER_SSH_KEY }}
          script: |
            cd /srv/repo/debian/dists/stable/main/binary-amd64
            dpkg-scanpackages . /dev/null | gzip -9c > Packages.gz

  build-and-deploy-arm64:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout repo
        uses: actions/checkout@v3

      - name: 🦀 Install Rust + ARM target
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-unknown-linux-gnu

      - name: 🧰 Install cross-compiling tools
        run: sudo apt update && sudo apt install -y dpkg-dev gcc-aarch64-linux-gnu

      - name: 🛠 Build Rust library for ARM64
        working-directory: src/rust/curp_verifier
        run: |
          cargo build --release --target aarch64-unknown-linux-gnu
          mkdir -p target/release
          cp target/aarch64-unknown-linux-gnu/release/libcurp_verifier.* target/release/

      - name: 📥 Install cbindgen
        run: cargo install cbindgen

      - name: 🧾 Generate header
        working-directory: src/rust/curp_verifier
        run: make cbindgen

      - name: 📦 Build .deb package for ARM64
        working-directory: src/rust/curp_verifier
        run: |
          make install DESTDIR=$(pwd)/deb_pkg
          mkdir -p deb_pkg/DEBIAN
          cp packaging/deb/arm64/control deb_pkg/DEBIAN/control
          dpkg-deb --build deb_pkg curp-verifier-arm64.deb
          ls -lh curp-verifier-arm64.deb
          ls -lah

      - name: 🚀 Upload ARM64 package to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.RAFEX_SERVER_HOST }}
          username: ${{ secrets.RAFEX_SERVER_USER }}
          key: ${{ secrets.RAFEX_SERVER_SSH_KEY }}
          source: "src/rust/curp_verifier/curp-verifier-arm64.deb"
          target: "/srv/repo/debian/dists/stable/main/binary-arm64/"
          debug: true

      - name: 🔄 Regenerate Packages.gz for ARM64
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.RAFEX_SERVER_HOST }}
          username: ${{ secrets.RAFEX_SERVER_USER }}
          key: ${{ secrets.RAFEX_SERVER_SSH_KEY }}
          script: |
            cd /srv/repo/debian/dists/stable/main/binary-arm64
            dpkg-scanpackages . /dev/null | gzip -9c > Packages.gz